import { defineConfig } from "astro/config";
export default defineConfig({
  base: "/astro/"
});


import { defineConfig } from 'astro/config';

export default defineConfig({
  // If you serve the site at http://yourdomain/astro/
  base: '/astro/',
  // Optional but recommended if you know your public URL:
  // site: 'http://18.220.33.0',
});
