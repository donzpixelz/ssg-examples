---
import BaseLayout from '../layouts/BaseLayout.astro';
---
<BaseLayout title="Page not found · Astro Example">
    <h1>404 — Not found</h1>
    <p>That page isn’t here. Try the <a href={import.meta.env.BASE_URL}>homepage</a> or the <a href={import.meta.env.BASE_URL + 'posts/'}>blog</a>.</p>
</BaseLayout>
