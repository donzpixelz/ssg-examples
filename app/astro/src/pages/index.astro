---
import DigitalClock from "../components/DigitalClock.jsx";
import AnalogClock from "../components/AnalogClock.jsx";
import Counter from "../components/Counter.jsx";
---

<!-- Page: React Examples -->
<section class="wrap">
    <header class="section-head">
        <h1>React Examples</h1>
        <ul class="bullets">
            <li><strong>Islands</strong>: hydrate only what needs JS.</li>
            <li><strong>Hydration</strong>: <code>client:load</code>, <code>client:visible</code>, <code>client:idle</code>.</li>
            <li><strong>Framework-agnostic</strong>: use React + Markdown.</li>
        </ul>
    </header>

    <div class="grid">
        <article class="card">
            <h2>Digital (React)</h2>
            <DigitalClock client:load />
        </article>

        <article class="card">
            <h2>Analog (React)</h2>
            <AnalogClock client:load />
        </article>

        <article class="card">
            <h2>Counter (React)</h2>
            <Counter client:load />
        </article>
    </div>
</section>

<style>
    .wrap { padding: 1rem; }
    .section-head { background: var(--bg-soft, #11161d); border-radius: 12px; padding: 1rem 1.25rem; margin-bottom: 1rem; }
    .section-head h1 { margin: 0 0 .25rem 0; }
    .bullets { margin: .25rem 0 0 0; padding-left: 1.25rem; }
    .grid {
        display: grid;
        grid-template-columns: 1fr;
        gap: 1rem;
    }
    @media (min-width: 980px){
        .grid { grid-template-columns: 1fr 1fr; }
        .grid .card:last-child { grid-column: 1 / -1; }
    }
    .card {
        background: #335fb6; /* matches your screenshot vibe */
        border: 2px solid rgba(255,255,255,.15);
        border-radius: 12px;
        padding: 1rem;
        box-shadow: inset 0 0 0 1px rgba(0,0,0,.2);
    }
    .card h2 { margin-top: 0; color: #fff; }
</style>
