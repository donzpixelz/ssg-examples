<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <title>{% if title %}{{ title }}{% else %}My 11ty Site{% endif %}</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <link rel="stylesheet" href="{{ '/css/main.css' | url }}">
  <style>
    body { font-family: system-ui, -apple-system, Segoe UI, Roboto, sans-serif; margin: 0; }
    header, main, footer { max-width: 960px; margin: 0 auto; padding: 1.5rem; }
    header { border-bottom: 1px solid #eee; }
    nav a { margin-right: 1rem; text-decoration: none; }
    footer { border-top: 1px solid #eee; color: #666; font-size: .9rem; }
  </style>
  <link rel="stylesheet" href="/eleventy/css/main.css?v=1">
</head>
<body>
<header class="header-outer">
  <h1>
    Eleventy
  </h1>
  <p>{% if title %}{{ title }}{% else %}<span class="font-eleventy">Eleventy</span>{% endif %}</p>
  <nav>
    <a href="{{ '/' | url }}">Home</a>
    <a href="{{ '/eleventy/' | url }}" class="eleventy-link"
       {% if page.url contains '/eleventy/' %}aria-current="page"{% endif %}>Eleventy</a>
    <a href="{{ '/about/' | url }}">About</a>
    <a href="{{ '/contact/' | url }}">Contact</a>
  </nav>
</header>




  <main>
    {{ content | safe }}
  </main>

  <footer>
    © My 11ty Site - 007 - <time id="page-loaded-at"></time>
  </footer>
</body>
</html>
