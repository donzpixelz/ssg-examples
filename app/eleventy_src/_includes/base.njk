<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <title>{% if title %}{{ title }}{% else %}Eleventy{% endif %}</title>
  <link rel="stylesheet" href="/eleventy/css/main.css">

  <style>
    /* --- minimal baseline so things look sane --- */
    :root{
      --brand:#3466cb;
      --text:#111;
      --paper:#fff;
      --ring:#e5e7eb;
      --muted:#6b7280;
      --radius:12px;
      --space:16px;
      --maxw:960px;
    }
    *{box-sizing:border-box}
    html,body{height:100%}
    body{
      margin:0;
      background:#f7f8fb;
      color:var(--text);
      font:16px/1.55 system-ui,-apple-system,Segoe UI,Roboto,Helvetica,Arial,"Apple Color Emoji","Segoe UI Emoji";
    }
    a{color:inherit;text-decoration:none}
    a:hover{text-decoration:underline}

    .header-outer{
      background:var(--brand);
      color:#fff;
      border-radius:var(--radius);
      padding:12px 16px;
      margin:24px auto;
      max-width:var(--maxw);
    }
    .header-outer h1{
      margin:0 0 8px;
      font-size:28px;
      line-height:1.2;
    }
    .header-outer nav{
      display:flex;
      gap:12px;
      flex-wrap:wrap;
      justify-content:center;
    }
    .header-outer nav a{
      display:inline-block;
      padding:6px 10px;
      border-radius:8px;
      background:rgba(255,255,255,.12);
    }
    .header-outer nav a:hover{background:rgba(255,255,255,.2)}
    .header-outer nav a.eleventy-link{font-weight:600}

    .container{
      max-width:var(--maxw);
      margin:0 auto 32px;
      padding:var(--space);
      background:var(--paper);
      border:1px solid var(--ring);
      border-radius:var(--radius);
      box-shadow:0 2px 8px rgba(0,0,0,.04);
    }

    /* Optional: make “Eleventy” stand out if you use <span class="font-eleventy"> */
    .font-eleventy{
      font-family:'Playball','Copperplate','Copperplate Gothic Light',fantasy,system-ui,sans-serif;
      letter-spacing:.2px;
    }

    /* Tidy default content spacing */
    .container :where(h1,h2,h3,h4){margin:1.2em 0 .5em}
    .container p{margin:.75em 0;color:var(--text)}
    img{max-width:100%;height:auto;display:block}
    code,pre{font-family:ui-monospace,SFMono-Regular,Menlo,Monaco,Consolas,"Liberation Mono","Roboto Mono",monospace}
  </style>
</head>
<body>
  <header class="header-outer">
    <h1>Eleventy Example</h1>
    <nav aria-label="Primary">
      <a href="/eleventy/" class="eleventy-link">Home</a>
      <a href="/eleventy/about/">About</a>
      <a href="/eleventy/contact/">Contact</a>
    </nav>
     <p style="text-align: center;">{% if title %}{{ title }}{% else %}<span class="font-eleventy">Eleventy</span>{% endif %}</p>
  </header>

  <main class="container">
    {{ content | safe }}
  </main>
</body>
</html>
